import { Text } from '@components';
import { View } from 'react-native';

export const LoginScreen: React.FC = () => {
  return (
    <View>
      <Text text="Login Screen" type="regular-base" />
    </View>
  );
};
